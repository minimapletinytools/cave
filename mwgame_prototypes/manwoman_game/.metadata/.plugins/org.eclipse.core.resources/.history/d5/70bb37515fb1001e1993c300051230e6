import pygame

def drawMask(surface):
    sfc = pygame.Surface((surface.get_width(),surface.get_height()))
    sfc.fill((0,0,0))
    pygame.draw.circle(sfc,pygame.Color(255,255,255,127),(200,200),50,0)
    surface.blit(sfc,(0,0))