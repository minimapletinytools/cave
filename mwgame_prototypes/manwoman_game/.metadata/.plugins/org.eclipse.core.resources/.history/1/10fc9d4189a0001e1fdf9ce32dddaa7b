import pygame

class SAnimator:
    def __init__(self,exml):
        imgDict = dict()
        for e in exml.childNodes:
            if e.nodeName == static:
        #load images from xml
        #set reverse images
    